
After completing your research, you MUST end your response with a fenced JSON block (nothing after it) in exactly this format:

```json
{
  "title": "...",
  "requirements": ["requirement 1", "requirement 2", "requirement 3"],
  "acceptance_criteria": ["criterion 1", "criterion 2"],
  "estimated_complexity": "simple",
  "notes": "..."
}
```

The JSON block must be the LAST thing in your response. `estimated_complexity` must be one of: `simple`, `medium`, `complex`.
